# Document covers

Place the extracted PNG cover images directly in this folder.

Expected paths follow this format:

`/public/covers/document-slug.png`

Do not place the images inside an additional nested `covers_clean` folder.
